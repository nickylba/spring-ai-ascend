# Module metadata for agent-client.
# Required by CLAUDE.md Rule 31 (Independent Module Evolution).
# Authority: ADR-0066 + ADR-0049 (Edge Access plane / Client SDK).
# Enforced by gate rule module_metadata_present_and_complete.

module: agent-client
kind: domain             # platform | domain | starter | bom | sample
version: 0.0.1-SNAPSHOT
semver_compatibility: experimental
description: "Edge Access plane — AgentClient SDK skeleton. HTTP client + Task Cursor consumer + SSE/Webhook receiver. Implementation deferred to W3+ per ADR-0049."
architecture_doc: agent-client/ARCHITECTURE.md
dfx_doc: docs/dfx/agent-client.yaml
spi_packages:
  - ascend.springai.client.spi   # placeholder; populated in W3+ per ADR-0049
allowed_dependencies: []  # standalone SDK; no runtime/platform deps
forbidden_dependencies:
  - agent-service           # post-Phase-C (consolidated from agent-platform per ADR-0078)
  - agent-runtime-core      # post-Phase-C (extracted from agent-runtime per ADR-0079)
  - agent-execution-engine  # post-Phase-C (extracted from agent-runtime per ADR-0079)
  - agent-middleware
  - agent-bus
  - agent-evolve
deployment_plane: edge    # Rule 39 / P-I — Edge Access plane

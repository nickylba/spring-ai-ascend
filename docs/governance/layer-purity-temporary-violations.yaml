# Layer-Purity Temporary Violations —the known, not-yet-migrated leaks.
#
# Authority: ADR-0159 (Progressive Learning Curve and Authority Lanes) §7 + §9.
# Companion policy: docs/governance/layer-purity-policy.yaml (the owns/forbids
#   category vocabulary every row here cites).
# Baseline-truth input: docs/governance/remediation-inventory/layer-purity-scan.md
#   (the line-referenced, advisory scan that classified each leak LEAKED vs
#   DEFENSIBLE against the adjudicated verdict). Every row below is copied from
#   a LEAKED row of that scan; this file invents no new locus and no new
#   category.
#
# What this file IS. The closed, dated grandfather list of L2/code detail that
#   currently sits in an L0 or L1 architecture document in violation of the
#   layer-purity policy, but has not yet been migrated to its authoritative
#   home (L2 / contracts / generated facts). Each entry freezes one known
#   violation AND declares a per-entry sunset_date by which the migration MUST
#   complete. A closed-but-dateless grandfather list is an anti-pattern (every
#   entry MUST carry a sunset_date); this list is closed (no new entry may be
#   added once the lane-purity gate is blocking —new leaks must be fixed at
#   authoring time, never grandfathered).
#
# What this file is NOT. A migration plan (the scan §4 holds the priority
#   ordering), a rule card, or an enforcer row. The future lane-purity gate
#   (G-27..G-33, authored + promoted by the remediation waves per ADR-0159 §9,
#   NOT by this file) consumes this list as its allow-list: a leaked-category
#   block whose locus matches a non-expired row here is tolerated; any leak
#   NOT listed here, or any row whose sunset_date has passed, fails the gate.
#   The authority cascade (generated facts > DSL > Card/prose) is untouched.
#
# Schema:
#   schema_version  - integer, bumped when fields are added.
#   authority       - the ratifying ADR id.
#   last_updated    - ISO yyyy-mm-dd (UTC). UTC convention (a local +08 date
#                     can be UTC-future and trip a freshness gate).
#   status          - advisory | blocking. Advisory until the lane-purity gate
#                     promotes to blocking per ADR-0159 §9. This is the LIST's
#                     posture, not a contract-status enum (Rule 62 does not
#                     apply —this is not a docs/contracts/*.v1.yaml).
#   list_closed     - true. No new violation may be grandfathered; new leaks
#                     are fixed at authoring time.
#   violations[]    - one row per known leak. Each row:
#       id          - stable kebab id, prefix LPV- (Layer-Purity Violation).
#       layer       - L0 | L1 (the over-layer the leak currently sits in).
#       file        - the offending authority document (module-relative path).
#       locus       - the smallest in-file anchor (section + line range) as
#                     recorded by the scan; for cross-reference, NOT a code
#                     fact.
#       category    - the leaked-category id from layer-purity-policy.yaml
#                     (one of L1..L8). Multiple categories -> categories[].
#       categories  - OPTIONAL list when a single block leaks more than one
#                     category (used instead of `category`).
#       trigger     - the smallest verbatim phrase that makes it a leak (from
#                     the scan). Quoted as EVIDENCE, never as a live claim.
#       migrate_to  - the authoritative home THIS row's detail must move to,
#                     resolved to a CONCRETE path that exists on disk today (an
#                     L2 sink directory, a contract surface, a Flyway migration
#                     directory, the rls-policy.sql design-only artifact, or a
#                     generated-fact file). NOT the category-template
#                     `architecture/docs/L2/<frame>/` form the sibling
#                     layer-purity-policy.yaml `home` column carries: that file
#                     states the per-CATEGORY home as a template; this file must
#                     state the per-ROW home as a resolved sink. A `migrate_to`
#                     that points at a path absent from disk is a dangling-target
#                     defect (the allow-list would reference a sink that resolves
#                     to nothing, weakening the authority chain Card/prose must
#                     not invent IDs/relationships). Every value below resolves
#                     on disk.
#       migrate_to_status - OPTIONAL enum. `resolved` (default, omitted) when the
#                     row's ideal L2 architecture-side sink already exists on disk
#                     and `migrate_to` names it. `sink-pending` when the row's
#                     ideal per-detail L2 sink directory is NOT yet built; in that
#                     case `migrate_to` names the concrete authority surface that
#                     DOES exist today (the channel contract) so the reference
#                     resolves, and this field records that the architecture-side
#                     L2 sink is the outstanding migration artefact the
#                     sunset_date governs. No current row carries `sink-pending`
#                     (the only prior user, the now-removed L0 event-buffer row,
#                     was deleted in the 2026-05-30 L0 dead-grandfather removal);
#                     the field is retained in the schema for the next row whose
#                     detail's dedicated L2 sink directory is not yet built.
#       sunset_date - REQUIRED ISO yyyy-mm-dd by which migration MUST complete
#                     and this row MUST be removed. Tiered by scan §4 priority:
#                     highest-priority L0 leaks 2026-06-30 (v1.0 boundary);
#                     L1 agent-service corpus 2026-07-31 (aligned with the
#                     saa-property-authority.yaml grandfather sunset already in
#                     flight); lower-priority deferred-row / other-module leaks
#                     2026-08-31.
#       scan_ref    - the scan section the row was copied from.
#       defensible_part - OPTIONAL note on the identity portion of a mixed
#                     block that MAY stay after the leaked detail is extracted.
#
# Change log (this file is an exempt surface for wave/log metadata):
#   2026-05-30 —agent-service L1 cleanup. The L2/code detail named by the
#     agent-service rows below was drained out of the L1 prose into the active
#     L2 sinks (architecture/docs/L2/run-http-contract/{logical,process,
#     development}.md) + the generated facts; the L1 4+1 views now carry only
#     structural layer/aggregate/SPI-boundary identity + obligation-level
#     delegation. Six rows whose file+category the advisory layer-purity gate
#     (gate/lib/check_layer_purity.py, E194) now scans LEAK-FREE were removed:
#     LPV-l1-as-test-inventory, LPV-l1-as-spi-signatures (the inlined
#     `Execute(TaskMetadata, InjectedContext) -> StateDelta` SPI signature was
#     replaced with a code-symbol/contract delegation), LPV-l1-as-process-
#     idempotency-status, LPV-l1-as-process-cas-chain,
#     LPV-l1-as-process-cancel-cas-sql, LPV-l1-as-scenarios-status. The
#     remaining agent-service rows are retained: the gate still detects a
#     same-category trigger token in each of those files, but the residual is a
#     DEFENSIBLE construct the regex cannot tell from a leak —a process-view
#     section heading / cross-link that NAMES a flow (e.g. "cancel-race loser
#     P6"), a Boundary-Contract delegation pointer that NAMES the migrated
#     detail to say it is delegated (e.g. "the RLS policy bodies ... is
#     delegated to development.md §5.3", "traceparent behaviour per ADR-0061"),
#     or an obligation-level statement ("RLS policy required") inside a Rule
#     G-1.1.c delegation frame. The dated allow-list is the sanctioned
#     tolerance for exactly such regex-indistinguishable residue.
#   2026-05-30 —L1 bus/engine/middleware cleanup (the L1 analogue of the
#     agent-service drain, for the three remaining domain modules). The
#     L2/code detail was drained out of the agent-execution-engine and
#     agent-middleware module-root + 4+1 views into the contracts
#     (engine-envelope.v1.yaml / engine-hooks.v1.yaml), the generated facts,
#     and the per-view process narratives; agent-bus/ARCHITECTURE.md was
#     re-grounded on boundary identity + an L2/engine-port-boundary/ pointer.
#     Two rows whose file+category the advisory layer-purity gate
#     (gate/lib/check_layer_purity.py, E194) now scans LEAK-FREE were removed:
#     LPV-l1-engine-strict-matching-outcome (the `engine_type=X ... Mismatch ->
#     EngineMatchingException -> Run.FAILED` dispatch-and-fail chain is now
#     delegated to the run-event + engine-envelope contracts and narrated at L1
#     in the process view) and LPV-l1-middleware-hook-ordering (the
#     `= registration order` mechanism is now owned by engine-hooks.v1.yaml).
#     The advisory re-run held 24 findings (none in any bus/engine/middleware
#     module-root or 4+1 view file —the new active per-view content is
#     leak-free) and dropped the grandfathered count by clearing these two
#     rows, introducing zero new findings. agent-bus carried no row.
#   2026-05-30 —locus + trigger re-validation (convergence scan round 2). The
#     in-place L0/L1 drains shifted content, so several retained rows cited
#     file:line evidence that no longer matched the file (root family
#     layer-purity-leakage / stale-grandfather-baseline-drift: trigger phrases
#     were stored as verbatim EVIDENCE but never re-validated after the drain
#     edits, leaving the allow-list untrustworthy to the lane-purity gate that
#     consumes it). This wave re-anchored each affected row to its LIVE locus and
#     reframed its trigger to the residue that is actually present now (a
#     delegation pointer / obligation-level statement / NAME-level section), not
#     the removed verbatim leak. Refreshed: (L0) LPV-l0-suspendreason-signatures
#     510-517 -> 537-538 (pure line drift; signatures unchanged); LPV-l0-shipped-
#     subset-test-inventory 77 -> 79 (W0 subset paragraph moved); LPV-l0-tenant-
#     rls-guc §4 #3 360 -> 348-373 (the SET LOCAL GUC was drained in-place; line
#     360 now holds the FP-TENANT-CROSS-CHECK code-symbol/ mechanism ref +
#     fp-tenant-cross-check L2 sink + rls-policy.sql design-only pointer; trigger
#     reframed to the delegation residue + defensible_part added). (L1 agent-
#     service) LPV-l1-as-logical-annotated-dfa 217-262 -> 176-217 (§3 heading
#     renamed to "Run lifecycle state machine (cancel-race-aware)"; updateIfNot
#     Terminal CAS / POST routes / 409 / RunStateMachine.java:37 / WHERE status
#     NOT IN drained; now a NAME-level stateDiagram + delegation to openapi-v1
#     .yaml + development.md §5.3); LPV-l1-as-process-cancel-race 259-286 -> 211-
#     241 (P6 "CAS WINS ... LOSES ... => 409" narrative drained; now a NAME-level
#     loser-path flowchart + delegation to openapi-v1.yaml / run-event.v1.yaml);
#     LPV-l1-as-development-rls-guc 239-248 -> 217-236 (the V?__tenant_rls.sql
#     series + SET LOCAL GUC + R2DBC wiring drained; line 239-248 is now the §5.4
#     DualTrackRouter zone; the RLS zone moved to the §5.3 Rule G-1.1.c Boundary
#     Contract delegation frame). No row was added or removed (each still detects
#     a same-category regex token that is regex-indistinguishable DEFENSIBLE
#     residue); only stale loci + triggers were corrected so the allow-list
#     evidence again matches the files it guards. The authority cascade is
#     untouched.
#   2026-05-30 — dangling migrate_to reconciliation (convergence scan round 2,
#     root family layer-purity-leakage / dangling-migrate-target). Every row's
#     `migrate_to` had been authored with an angle-bracket placeholder frame
#     slug (`architecture/docs/L2/<run-state-frame>/`, `<idempotency-frame>/`,
#     `<tenant-isolation-frame>/`, `<hook-dispatch-frame>/`, `<event-channel-
#     frame>/`, `<access-layer-frame>/`, `<agent-service frames>/`) copied from
#     the layer-purity-policy.yaml `home` CATEGORY template, before the L2
#     directory was populated. The concrete L2 sinks that now exist on disk are
#     named differently, so no row resolved to its destination. This wave
#     resolved each placeholder to the concrete on-disk sink: `<run-state-frame>`
#     -> `architecture/docs/L2/fp-run-state-transition/`; `<idempotency-frame>`
#     -> `fp-idempotency-claim/`; `<hook-dispatch-frame>` -> `fp-hook-dispatch/`;
#     `<tenant-isolation-frame>` -> `fp-tenant-cross-check/` (the home the
#     already-reconciled LPV-l0-tenant-rls-guc row adopted) + `rls-policy.sql` +
#     Flyway migrations; `<access-layer-frame>` -> `run-http-contract/
#     development.md` (filter-chain order owner); `<agent-service frames>` ->
#     `run-http-contract/process.md` (sequence view). The one detail whose
#     dedicated L2 sink directory is genuinely NOT yet built — the bounded
#     event-buffer channel (LPV-l0-event-buffer-overflow) — was pointed at the
#     concrete `docs/contracts/run-event.v1.yaml` channel contract that DOES
#     exist and marked `migrate_to_status: sink-pending` (new optional field) so
#     the reference resolves and the outstanding architecture-side sink is
#     recorded honestly. No row was added or removed; no locus, trigger, or
#     category changed; only the dangling migrate targets were resolved. The
#     authority cascade (generated facts > DSL > Card/prose) is untouched.
#   2026-05-30 — L0 dead-grandfather removal (convergence scan round 2, root
#     family layer-purity-leakage / stale-grandfather-baseline-drift). The prior
#     "locus + trigger re-validation" entry above re-anchored the six L0-root rows
#     to their §4 constraint loci, but the constraint BODIES had already been
#     drained in place (to identity + delegation + fact-ID form), so under the
#     locus-anchored gate (gate/lib/check_layer_purity.py, E194 — a row tolerates a
#     leak only when the leak's line falls inside the row's locus range) each of
#     those six rows covered ZERO live leak. A grandfather row that tolerates no
#     live leak is dead weight that OVER-CLAIMS a violation the list's own
#     discipline forbids, so the six were REMOVED, not re-kept:
#       - LPV-l0-run-dfa-table          (§4 #20) — the PENDING->RUNNING transition
#         table, the `withStatus(..) MUST invoke RunStateMachine.validate(..)` call
#         chain, the 200/409 cancel-idempotency codes, and the `persisted.version
#         != run.version()-1` CAS predicate are gone; §4 #20 now delegates to the
#         fp-run-state-transition L2 sink + `contract-op/cancelrun` via fact-IDs.
#       - LPV-l0-tenant-rls-guc         (§4 #3)  — `SET LOCAL app.tenant_id` + the
#         "enable Postgres RLS policies" mechanism are gone; §4 #3 now delegates to
#         FP-TENANT-CROSS-CHECK (code-symbol mechanism) + the fp-tenant-cross-check
#         L2 sink + the `docs/security/rls-policy.sql` design-only artifact.
#       - LPV-l0-idempotency-status-table (§4 #4) — the `Idempotency-Key header`,
#         the 400/409 status codes, and the `idempotency_dedup` table are gone; §4
#         #4 now delegates to `contract-op/createrun` + the fp-idempotency-claim L2
#         sink.
#       - LPV-l0-hook-chain-ordering    (§4 #16) — the `@Order` ordering algorithm
#         and the `Class.getName()` lexicographic tie-break are gone; §4 #16 now
#         delegates to `engine-hooks.v1.yaml` + the fp-hook-dispatch L2 sink.
#       - LPV-l0-event-buffer-overflow  (§4 #28) — the `default 64 events,
#         DROP_OLDEST overflow` wire shape is gone; the bounded-buffer obligation
#         is stated as an invariant and the channel detail is owned by
#         `docs/contracts/run-event.v1.yaml`.
#       - LPV-l0-shipped-subset-test-inventory (§1 W0) — the inline contract-test
#         enumeration is no longer a leak the locus-anchored gate detects at the W0
#         paragraph (E194's L8 probe requires a table/bullet inventory, not a prose
#         comma-run; the line-anchored row tolerated nothing). The detail's home
#         stays `architecture/facts/generated/tests.json`.
#     VERIFIED (WSL, both helpers --mode advisory; both stay exit 0 / non-blocking).
#     For the LOCUS-ANCHORED gate (gate/lib/check_layer_purity.py, E194) removing
#     the six changed NO verdict — none of the six loci covered a detected leak, so
#     the finding/grandfathered counts are identical before and after (17 findings /
#     18 grandfathered corpus-wide; 6 L0-root findings / 4 L0-root grandfathered,
#     the 4 being the retained suspendreason 537-541 block). For the FILE+CATEGORY
#     gate (gate/lib/check_l2_detail_sink.py, E195), which is NOT yet locus-aware
#     (its grandfather match is per-file-per-category, the same blunt scope E194 has
#     since shed), the six rows had been masking incidental same-category tokens
#     elsewhere in the L0 file; removing them therefore RE-SURFACES ~5 previously-
#     grandfathered E195 advisory lines (79, 252, 394, 597, 1019) as advisory
#     findings — the residual being REVEALED, not created (see the scope note). The
#     lone L0-root survivor LPV-l0-suspendreason-signatures still tolerates a LIVE
#     leak (the sealed-variant constructor signatures at lines 537-541) and is kept.
#     SCOPE NOTE (non-silent residual). The L0-root findings that remain (E194) or
#     re-surface (E195) after this removal are NONE of them a leak any removed row
#     was named for: (a) one false positive — the `Glue LOC target <= 1 500` line,
#     where the L4 probe reads `500` as an HTTP status; (b) the §0.6 "known
#     lower-altitude residue" reading-bridge paragraph, which re-quotes the drained
#     §4 trigger phrases AS grandfathered debt; (c) the telemetry/trace vertical
#     residue at §4 #22 / #53-#55 + the §1 metric-naming `gen_ai.*` / `langfuse.*`
#     namespace (Flyway filename, `traceparent`/`traceresponse` mentions); and, for
#     E195 only, (d) two `test_inventory` lines — the §1 W0 contract-test comma-run
#     (line 79) and the §2 module-dependency ArchUnit-enforcer CITATION (line 252,
#     a D3-defensible mechanism citation that E194's L8 probe correctly exempts but
#     E195's blunter probe over-matches). These are a DISTINCT residual (the
#     telemetry-vertical altitude drain + the §0.6 bridge wording + a probe
#     false-positive + an E195 locus-awareness gap), owned by a separate remediation
#     pass / the reconcile step, NOT by these six run/idempotency/hook/buffer/test
#     rows; they are recorded here so the gap is not silent. Both gates are advisory
#     and never block, so none of this gates CI. The durable fix for the E195
#     re-surfacing is to teach E195 the same locus anchoring E194 already has (a
#     gate-precision change, out of scope for this list edit), NOT to re-add a dead
#     whole-file grandfather row. The authority cascade (generated facts > DSL >
#     Card/prose) is untouched.
#   2026-05-30 — encoding note (housekeeping). An earlier wave on this branch had
#     written part of this file with a GBK->UTF-8 mis-encoding that corrupted some
#     `§`/`—` glyphs (a Windows editor encoding bug). The convergence-scan-round-2
#     edits restored correct UTF-8; the whole file now parses as clean UTF-8 with
#     no mojibake. Recorded so a future reader knows the glyph corruption was a
#     transient authoring artefact, not an intended encoding.
#   2026-05-30 — L0 telemetry-band live-leak freeze (completeness) (convergence
#     scan round 2, root family layer-purity-leakage / closed-and-incomplete-
#     grandfather-list). The list was CLOSED but INCOMPLETE relative to the
#     by-fact-id leak set: it froze the heaviest L0 leaks yet left the telemetry
#     band's LIVE loci unlisted, so the advisory locus-anchored gate
#     (gate/lib/check_layer_purity.py, E194) reported them as non-grandfathered
#     findings — the residual the prior "L0 dead-grandfather removal" SCOPE NOTE
#     (c) flagged for "a separate remediation pass / the reconcile step". This
#     wave is that pass. Four rows were ADDED, each covering a leak the gate
#     detects TODAY (so each earns its place — a row over zero live leak is not
#     added, per the same discipline that removed the six dead rows above):
#       - LPV-l0-metric-span-attr-namespaces   (§4 #5 : 392-395, L6) — the
#         `gen_ai.*` / `langfuse.*` span-attribute namespaces inlined in the
#         metric-naming constraint; the L6 probe fires on the literal tokens.
#       - LPV-l0-run-context-accessor-residual (§4 #22 : 575-602, L4) — the
#         drained accessor-inventory delegation prose still NAMES `traceparent`
#         propagation; the L4 header probe fires on that DEFENSIBLE residue.
#       - LPV-l0-run-trace-id-notnull          (§4 #54 : 1019, L3+L7) — the
#         `V2__run_trace_id_notnull.sql` Flyway ref + the enumerated `RunContext`
#         trace/span/session accessor list; the L3 probe fires on the V__*.sql.
#       - LPV-l0-traceparent-edge-residual     (§4 #55 : 1021, L4) — the drained
#         propagation-mechanics delegation prose still NAMES the `traceparent`
#         header; the L4 header probe fires on that DEFENSIBLE residue.
#     Also re-validated the lone survivor LPV-l0-suspendreason-signatures: its
#     locus widened 537-538 -> 537-541 so the AwaitTimer/AwaitExternal/Await
#     Approval/RateLimited sealed-variant lines (539-541) — a live L7 finding the
#     prior range under-covered — are now tolerated by the row that exists for
#     exactly that leak.
#     NOT added (drained to zero live leak by this same round-2 wave — the gate's
#     regex no longer fires, so a grandfather row would be dead weight the unused-
#     row housekeeping flags): §4 #25 (CausalPayloadEnvelope shape), #56
#     (GENERATION span attribute schema), #57 (per-span `tenant.id` + 403 replay
#     fail-closed), #58 (no-PII `payload_ref://` invariant), #59 (MCP replay-tool
#     inventory). Their detail now delegates to docs/telemetry/policy.md + the
#     telemetry-vertical L2 sink + the generated facts; if a future probe tightens
#     to catch a dotted attribute namespace or an MCP tool list, the migration
#     (not a new grandfather row) is the fix, per closed-list discipline. The §4
#     #6 `Glue LOC target <= 1 500` finding is a probe false-positive (the L4
#     probe reads `500` as an HTTP status), owned by a gate-probe fix, not this
#     allow-list. VERIFIED (WSL, --mode advisory): the four added loci now report
#     GRANDFATHERED, not as findings; no row over zero live leak was added. The
#     authority cascade (generated facts > DSL > Card/prose) is untouched.
#   2026-05-30 — convergence scan round 3 detector-recoupling + L0 §4 drain
#     reconcile (root family layer-purity-leakage / detector-inventory-coupling +
#     stale-grandfather-baseline-drift). Round 3 widened the TRIGGERS library
#     (gate/lib/check_layer_purity.py) to catch surface forms the prior probes
#     missed — a `via Type.method(` call hop, a slash/comma + lifecycle method-name
#     RUN, a `deadline() : Instant` colon-return signature, and a record-component
#     constructor with field-name args. Two of those probes initially OVER-fired on
#     defensible look-alikes (a prose-paren `via Type.member (annotation)` hop and a
#     file PATH `architecture/docs/L0/...` mis-read as a method-name run); the fix
#     tightened them to require the call paren to ABUT the name and the run slashes
#     to be whitespace-flanked with no path char — a probe defect, not a leak to
#     grandfather. The widened probes then detected three §4 leaks in the L0 prose
#     that the prior probes missed (§4 #8 `ResiliencePolicy(cbName, retryName,
#     tlName)`, §4 #19 `deadline() : Instant`, §4 #9 `via Checkpointer.save()` /
#     `RunRepository.save()`), each of which was drained in place to a
#     boundary-identity + L2/SPI-fact delegation (the corrected-altitude content the
#     standing direction calls for), and the verbose §4 #27 Skill-SPI invariant
#     prose was condensed in the same pass to keep the file near the always-loaded
#     budget. Those in-place drains shifted the §4 line numbers, so the four
#     retained L0 telemetry/SPI rows were RE-ANCHORED to their live loci:
#     LPV-l0-suspendreason-signatures 537-541 -> 546-551; LPV-l0-run-context-
#     accessor-residual 575-602 -> 584-614 (and given a second category L6-wire-
#     format so E195's wire-family reading of the same `traceparent` token at the
#     drained line 606 is tolerated by the row that already covers it for E194);
#     LPV-l0-run-trace-id-notnull 1019 -> 1036; LPV-l0-traceparent-edge-residual
#     1021 -> 1038. One row was ADDED — LPV-l0-skill-spi-lifecycle-run — for the
#     §4 #27 `init / execute / suspend / teardown` lifecycle run the widened L1
#     probe newly detects: the L0 prose is already a delegation pointer NAMING the
#     run to say its shape is fact/contract-owned, so the residual is a regex-
#     indistinguishable DEFENSIBLE delegation pointer the dated allow-list tolerates
#     (it covers a leak the WIDENED gate detects today, so it earns its place; no
#     row over zero live leak was added). The L1 agent-service §8.2 risk note that
#     NAMED `Filter ordering` (a NEW line the L1 drain added) was reworded to name
#     the RISK (edge filter-chain composition) + delegate the concrete ordering to
#     the L2 design, NOT grandfathered — a leak the wave itself introduced is fixed
#     at authoring time, never grandfathered (closed-list discipline). VERIFIED
#     (WSL): E194 0 findings / 34 grandfathered; E195 0 findings / 4 grandfathered;
#     render-idempotency byte-identical (root + agent-service templates re-synced to
#     their drained outputs). The authority cascade is untouched.
#   2026-05-31 — C3a L0 genuine-migration drain (residual-cleanup; root family
#     F-layer-gate-flags-delegation-pointer — the genuine-leak arm of its (a)/(b)/(c)
#     promotion rubric). The live grandfathered set was classified row-by-row and the
#     three L0-root rows over a GENUINE live inline leak were DISCHARGED by migrating
#     the inlined detail to its authoritative home, leaving only boundary identity +
#     a delegation pointer in the L0 prose:
#       - LPV-l0-suspendreason-signatures (§4 #19) — the sealed-`SuspendReason`
#         variant-constructor inventory drained to the per-variant
#         `code-symbol/…-suspendreason-*` SPI-shape facts' public_methods[] /
#         record_components (the inline list had also DRIFTED from the shipped facts:
#         `ChildRun`+`AwaitChildren` vs the shipped single `awaitchild`, the omitted
#         `awaitclientcallback`, and `RateLimited(resourceKey, retryAfter)` vs the
#         shipped `ratelimited(skill, code)` — so the migration also closes a
#         stale-inventory defect; the drain does NOT transcribe the drifted names).
#       - LPV-l0-run-trace-id-notnull (§4 #54) — the `V2__run_trace_id_notnull.sql`
#         Flyway name, the trace_id column nullability transition, and the
#         `traceId()/spanId()/sessionId()/traceContext()` accessor enumeration drained
#         to the Flyway migrations + the `code-symbol/…-bus-spi-engine-runcontext`
#         fact + the telemetry-vertical L2 sink (the enumerated `traceId()`/`spanId()`
#         are not in the runcontext fact's public_methods, another drift the delegation
#         removes by not re-asserting the names).
#       - LPV-l0-metric-span-attr-namespaces (§4 #5) — the inline `gen_ai.*` /
#         `langfuse.*` span-attribute namespaces drained to §4 #56 (their real owner)
#         + docs/telemetry/policy.md; the `springai_ascend_` metric-prefix reservation
#         stays as the durable L0 invariant.
#     LPV-l0-suspension-write-atomicity (§4 #23) was REMOVED as a dead row: its body
#     had already drained in a prior wave, so it tolerated ZERO live leak under both
#     E194 (locus-anchored) and E195 — per the closed-list discipline a row over zero
#     live leak is removed, not kept. The retained L0-root rows are DEFENSIBLE
#     delegation-pointer residue (LPV-l0-run-context-accessor-residual §4 #22,
#     LPV-l0-traceparent-edge-residual §4 #55, LPV-l0-skill-spi-lifecycle-run §4 #27,
#     and the E195-side LPV-l0-shipped-subset-test-inventory); the §4 #19 drain shifted
#     §4 line numbers, so those rows were re-anchored to their LIVE loci
#     (run-context-accessor-residual 584-614 -> 582-612; traceparent-edge-residual
#     1038 -> 1036). NOTE the L0 root ARCHITECTURE.md is TEMPLATE-RENDERED from
#     docs/governance/templates/root-architecture.md.j2: each drain was applied to the
#     `.j2` template AND the rendered `.md` in lockstep so the byte-identical
#     render-idempotency gate (Rule G-13) stays green and a future re-render cannot
#     re-introduce the migrated detail. VERIFIED (WSL): E194 0 findings / 28
#     grandfathered; E195 0 findings / 2 grandfathered; render-idempotency
#     byte-identical; layer-purity self-tests 47/47; full architecture-sync gate PASS.
#     The authority cascade (generated facts > DSL > Card/prose) is untouched.
#   2026-05-31 — C4 closeout dead-grandfather removal (residual-cleanup; root family
#     layer-purity-leakage / stale-grandfather-baseline-drift). Per-row raw-coverage
#     of EVERY open row was measured against the live corpus using the helpers' own
#     matching logic (file + layer + category + locus for E194 via suppressing_row's
#     predicate; file + family->LP-category + locus for E195 via _grandfathered's
#     predicate), independent of the gate's first-match preference. SEVEN L1
#     agent-service rows covered ZERO live finding on BOTH E194 and E195 — their
#     adjudicated inline leak had been genuinely DRAINED by the agent-service /
#     bus-engine-middleware L1 cleanups (2026-05-30) and the C3a drain, and every
#     residual same-category token still in their file is absorbed by a still-live
#     anchorless whole-file row (L3 by LPV-l1-as-idempotency-sql /
#     LPV-l1-as-logical-rls-tables; L4 by LPV-l1-as-runs-api-matrix). A row over zero
#     live leak is dead weight that OVER-CLAIMS a violation the closed-list discipline
#     forbids, so the seven were REMOVED, not re-kept:
#       - LPV-l1-as-idempotency-hash-status (§2.A : 71-74, L3+L4) — the
#         `hashes method:path:body (SHA-256 -> base64url) ... 409 idempotency_conflict`
#         hash/collision-status leak is drained; the file's residual L3 (lines 19,397)
#         is covered by LPV-l1-as-idempotency-sql and L4 (line 94) by
#         LPV-l1-as-runs-api-matrix.
#       - LPV-l1-as-tenant-filter-order (§2.A : 128-134, L4+L5) — the
#         `JwtTenantClaimCrossCheck (L1, order 15 ...) ... 403 tenant_mismatch`
#         filter-order/status leak is drained; no L5 leak remains in the file and the
#         lone L4 token (line 94) is covered by LPV-l1-as-runs-api-matrix.
#       - LPV-l1-as-observability-filter-wire (§2.A : 199-218, L4+L5+L6) — the
#         `TraceExtractFilter ... order 10 ... traceresponse: 00-<trace_id>-...`
#         wire/order leak is drained; no L5/L6 leak remains and the L4 token (line 94)
#         is covered by LPV-l1-as-runs-api-matrix.
#       - LPV-l1-as-l2-linkage-rls (L2 Constraint Linkage : 797-799, L3) — the
#         `Postgres RLS migration sequence` leak under the L2-linkage heading is
#         drained; the file's residual L3 (lines 19,397) is covered by
#         LPV-l1-as-idempotency-sql.
#       - LPV-l1-as-logical-cas-chain (logical.md 31,39,67, L1+L3) — the
#         `RunRepository (atomic CAS via updateIfNotTerminal) / ... invoked via Layer 2
#         CAS` method-call/CAS leak is drained; no L1-method-call-chain leak remains in
#         logical.md and the L3 token (line 169) is covered by
#         LPV-l1-as-logical-rls-tables.
#       - LPV-l1-as-logical-rls-chain (logical.md 92-96, L3+L1) — the
#         `Persistence under RLS ... RunStateMachine.validate(from, to) is invoked`
#         leak is drained; same coverage as the row above (no L1 leak; L3 line 169
#         covered by LPV-l1-as-logical-rls-tables).
#       - LPV-l1-as-spi-appendix-shapes (spi-appendix.md, L7) — the SPI method-body /
#         parameter-list leak is fully drained: spi-appendix.md carries ZERO leak of
#         any category anywhere in the file (genuine drain, not locus drift).
#     VERIFIED (WSL): removing the seven changed NO verdict — both gates report the
#     IDENTICAL count before and after (E194 0 findings / 28 grandfathered; E195 0
#     findings / 2 grandfathered), because each removed row suppressed nothing. No row
#     was removed whose locus still covered a live leak, and no still-live leak was
#     left un-grandfathered (every same-category token in the affected files remains
#     covered by a retained anchorless whole-file row). The retained rows are exactly
#     those that tolerate >=1 live finding on the current disk state. No L0/L1
#     document was edited — this is a pure allow-list prune; the facts/DSL/graph and
#     the authority cascade (generated facts > DSL > Card/prose) are untouched.

schema_version: 1
authority: ADR-0159
last_updated: 2026-05-31
status: advisory
list_closed: true

violations:

  # ======================================================================
  # L0 root —architecture/docs/L0/ARCHITECTURE.md (scan §1.2)
  # Highest migration priority; sunset 2026-06-30 (v1.0 boundary).
  # ======================================================================

  # Six L0-root rows were REMOVED 2026-05-30 (convergence scan round 2 — see the
  # change-log entry "L0 dead-grandfather removal"): LPV-l0-run-dfa-table (§4 #20),
  # LPV-l0-tenant-rls-guc (§4 #3), LPV-l0-idempotency-status-table (§4 #4),
  # LPV-l0-hook-chain-ordering (§4 #16), LPV-l0-event-buffer-overflow (§4 #28), and
  # LPV-l0-shipped-subset-test-inventory (§1 W0). Their NAMED §4/§1 leaks were
  # drained in place to identity + delegation + fact-ID form, so under the
  # locus-anchored gate (gate/lib/check_layer_purity.py, E194) each row's locus
  # range now covers ZERO live leak. Per the closed-list discipline a row that
  # tolerates no live leak is removed, not kept. (The 2026-05-31 residual-cleanup
  # C3a wave then DISCHARGED the remaining genuine L0 inline leaks by migration —
  # LPV-l0-suspendreason-signatures §4 #19, LPV-l0-run-trace-id-notnull §4 #54,
  # LPV-l0-metric-span-attr-namespaces §4 #5 — and removed the dead
  # LPV-l0-suspension-write-atomicity §4 #23 row; see the change-log entry "C3a —
  # L0 genuine-migration drain". The L0-root rows that remain are DEFENSIBLE
  # regex-residue: LPV-l0-run-context-accessor-residual, LPV-l0-traceparent-edge-
  # residual, LPV-l0-skill-spi-lifecycle-run, and the E195-side
  # LPV-l0-shipped-subset-test-inventory.)

  # LPV-l0-suspendreason-signatures was DISCHARGED + removed 2026-05-31
  # (residual-cleanup C3a — genuine migration). The §4 #19 inline sealed-variant
  # constructor inventory (`ChildRun(UUID childRunId, ChildFailurePolicy, Instant
  # deadline) | AwaitChildren(...) | AwaitTimer(...) | ...`) was drained out of the
  # L0 prose: §4 #19 now states the suspend-deadline / EXPIRED invariant + names
  # the sealed `SuspendReason` as a boundary identity, and delegates the per-variant
  # constructor shapes + the deadline-accessor signature + the join / child-failure
  # enums to the SPI-shape facts rooted at
  # `code-symbol/com-huawei-ascend-service-runtime-resilience-spi-suspendreason`
  # (the sealed interface fact + its per-variant `…-suspendreason-*` facts'
  # `public_methods[]` / `record_components`). The inline enumeration had DRIFTED
  # from the shipped facts (it listed `ChildRun`+`AwaitChildren` as two variants
  # where the code ships one `awaitchild`, omitted the shipped `awaitclientcallback`
  # variant, and gave `RateLimited(String resourceKey, Instant retryAfter)` where
  # the shipped `ratelimited` record components are `skill` + `code`), so the
  # delegation also removes a stale-inventory defect. The JVM-typed-parameter probe
  # no longer fires on §4 #19. VERIFIED (WSL, E194 --mode advisory): 0 findings at
  # the §4 #19 block.

  - id: LPV-l0-shipped-subset-test-inventory
    layer: L0
    file: architecture/docs/L0/ARCHITECTURE.md
    locus: "§1 W0 shipped-subset contract-truth test enumeration (row-level pass deferred — whole-file: the inline comma-run of contract-truth test classes is detected by the E195 inline-test-list probe, which is NOT locus-aware, and the §1 paragraph shifts line as the L0 prose is rewritten, so a fixed line anchor cannot track it; whole-file tolerance is the sanctioned mechanism until the enumeration migrates)"
    category: L8-test-class-inventory
    trigger: "contract-truth tests (OpenApiContractIT, ApiCompatibilityTest, OrchestrationSpiArchTest, TenantPropagationPurityTest)"
    migrate_to: "architecture/facts/generated/tests.json (+ FunctionPoint test_refs[])"
    sunset_date: 2026-06-30
    scan_ref: "§1 W0 shipped subset"
    defensible_part: "Naming the shipped contract-truth tests as evidence of the W0 release is a reasonable L0 grounding claim; the enumerated test-class inventory is what migrates to tests.json + the FunctionPoint test_refs. This row is the E195-side tolerance (E195 maps test_inventory -> L8-test-class-inventory and matches on file + category, not locus); it is anchorless so it also covers the E194 L8 probe should the comma-run ever trip it."

  # LPV-l0-suspension-write-atomicity was REMOVED 2026-05-31 (residual-cleanup
  # C3a — dead-grandfather removal). The §4 #23 named persistence-write hops
  # (`RunRepository.save` + `checkpointer.save`), the per-tier atomicity ordering
  # (sequential-on-call-stack / one @Transactional block / transactional outbox),
  # and the suspend-leg sequence had already been drained in place to the L2
  # FunctionPoint sink `../L2/fp-suspend-resume/`; §4 #23 now carries only the
  # structural all-or-nothing observability invariant. Under both the
  # locus-anchored gate (E194) and the file+category gate (E195) the §4 #23 locus
  # covered ZERO live leak (the L1/L2 probes no longer fire there), so per the
  # closed-list discipline a row that tolerates no live leak is removed, not kept.
  # VERIFIED (WSL, both helpers --mode advisory): no finding/grandfathered hit at
  # the §4 #23 block.

  # ----------------------------------------------------------------------
  # L0 telemetry-band residue (convergence scan round 2, 2026-05-30; partially
  # discharged 2026-05-31 by residual-cleanup C3a).
  #
  # Originally four telemetry-band loci were frozen here. The 2026-05-31 C3a wave
  # genuinely DISCHARGED two of them by migration — LPV-l0-metric-span-attr-
  # namespaces (§4 #5 `gen_ai.*`/`langfuse.*` → §4 #56 + telemetry policy) and
  # LPV-l0-run-trace-id-notnull (§4 #54 Flyway filename + accessor enumeration →
  # facts + Flyway + telemetry-vertical L2) — leaving the two whose residue is a
  # DEFENSIBLE delegation pointer NAMING the migrated `traceparent` mechanism:
  # LPV-l0-run-context-accessor-residual (§4 #22) and LPV-l0-traceparent-edge-
  # residual (§4 #55). Each remaining row covers a leak the gate detects TODAY (a
  # row over zero live leak is not kept). They share the 2026-06-30 v1.0-boundary
  # sunset of the L0 band.
  #
  # Scope note. The §4 #25 envelope shape, #56 GENERATION span schema, #57 span
  # tenant attribute, #58 no-PII invariant, and #59 MCP replay-tool inventory
  # were DRAINED in place by the same round-2 wave (their detail now delegates to
  # docs/telemetry/policy.md + the telemetry-vertical L2 sink + the generated
  # facts); the gate's regex no longer fires on them, so under the closed-list
  # discipline they are NOT grandfathered here (a row over zero live leak would
  # be dead weight the gate's unused-row housekeeping flags for removal). #23 was
  # covered by LPV-l0-suspension-write-atomicity (now removed 2026-05-31 — see the
  # C3a removal note above; §4 #23 carries no live leak).
  # ----------------------------------------------------------------------

  # LPV-l0-metric-span-attr-namespaces was DISCHARGED + removed 2026-05-31
  # (residual-cleanup C3a — genuine migration). The §4 #5 inline span-attribute
  # namespaces (`gen_ai.*` / `langfuse.*`) were drained out of the metric-naming
  # constraint: §4 #5 now keeps the durable `springai_ascend_` metric-prefix
  # reservation invariant and delegates the concrete span-attribute namespace
  # convention to §4 #56 (its real owner, which already delegates to
  # docs/telemetry/policy.md §4 + the telemetry-vertical L2 sink). The L6
  # `gen_ai.*`/`langfuse.*` probe no longer fires on §4 #5. VERIFIED (WSL, E194 +
  # E195 --mode advisory): 0 findings at the §4 #5 block.

  - id: LPV-l0-run-context-accessor-residual
    layer: L0
    file: architecture/docs/L0/ARCHITECTURE.md
    locus: "§4 #22 : 582-612"
    categories: [L4-http-status-route-verb, L6-wire-format]
    trigger: "OTel traceparent propagation across suspend ... the concrete accessor inventory and its return types - the session / trace correlation accessors on the run context and the TraceContext it exposes - are the public_methods[] entries of the generated SPI-shape facts code-symbol/...runcontext and code-symbol/...tracecontext, not enumerated here (a delegation pointer that NAMES the migrated accessor mechanism + the traceparent propagation to say they are delegated)."
    migrate_to: "architecture/facts/generated/code-symbols.json + architecture/docs/L2/telemetry-vertical/"
    sunset_date: 2026-06-30
    scan_ref: "§1.2 (telemetry band)"
    defensible_part: "The §4 #22 accessor inventory was drained in-place: the constraint states only the structural invariant (the run context is the single in-kernel carrier of tenant/session/trace correlation, isolated from the HTTP edge) and delegates the typed accessor shapes to the runcontext / tracecontext SPI-shape facts' public_methods[]. The residual the regex matches is the named `traceparent` propagation in that delegation prose, regex-indistinguishable from a live header-behaviour leak (E194 reads it L4 header-parse/emit; E195's wire family reads the same named-header token as L6 wire-format, so the row carries both categories). Locus re-anchored 584-614 -> 582-612 to track the LIVE §4 #22 block after the 2026-05-31 C3a §4 #19 drain shifted the constraint up by two lines (the traceparent residue is now at line 604); the structural carrier commitment stays, the accessor inventory is already fact-owned."

  # LPV-l0-run-trace-id-notnull was DISCHARGED + removed 2026-05-31
  # (residual-cleanup C3a — genuine migration). The §4 #54 inline Flyway
  # migration-file name (`V2__run_trace_id_notnull.sql`), the trace_id column
  # nullability transition, and the enumerated `RunContext` accessor list
  # (`traceId()`/`spanId()`/`sessionId()`/`traceContext()`) were drained out of the
  # L0 prose: §4 #54 now states only the structural N:M Trace↔Run↔Session
  # correlation-identity invariant and delegates the persisted-column shape + the
  # NOT-NULL Flyway migration to the Flyway migrations + the
  # `../L2/telemetry-vertical/` sink, and the correlation accessors to the
  # `public_methods[]` of `code-symbol/com-huawei-ascend-bus-spi-engine-runcontext`.
  # (The drained accessor enumeration had also drifted from the shipped fact — the
  # runcontext fact's public_methods carry `sessionId()`/`tenantId()`/
  # `traceContext()` but not a separate `traceId()`/`spanId()` — so delegating to
  # the fact, rather than re-asserting the names, removes a stale-inventory defect;
  # the migration does not transcribe the accessor names.) The L3 V__*.sql probe no
  # longer fires on §4 #54. VERIFIED (WSL, E194 + E195 --mode advisory): 0 findings
  # at the §4 #54 block.

  - id: LPV-l0-traceparent-edge-residual
    layer: L0
    file: architecture/docs/L0/ARCHITECTURE.md
    locus: "§4 #55 : 1036"
    category: L4-http-status-route-verb
    trigger: "MUST extract or originate the W3C traceparent trace-context header on every inbound request ... The concrete propagation mechanics - the filter-chain position relative to the JWT/Tenant/Idempotency filters, the outbound trace-correlation response header and its wire grammar, the response-status scope, the Logback MDC slice, and the invalid-header metric - are runtime/wire detail owned by docs/telemetry/policy.md §6 and the L2 sink ../L2/telemetry-vertical/process.md (a delegation pointer that NAMES the traceparent header to say its mechanics are delegated)."
    migrate_to: "architecture/docs/L2/telemetry-vertical/ + architecture/docs/L2/run-http-contract/development.md + docs/telemetry/policy.md"
    sunset_date: 2026-06-30
    scan_ref: "§1.2 (telemetry band)"
    defensible_part: "The §4 #55 propagation mechanics were drained in-place: the constraint owns only the structural obligation (the HTTP edge participates in W3C trace-context propagation and fails closed on an unparseable id) and delegates the filter position, the traceresponse wire grammar, the response-status scope, the MDC slice, and the invalid-header metric to the telemetry policy + the telemetry-vertical L2 process view. The residual the L4 regex matches is the named traceparent header in that delegation prose, regex-indistinguishable from a live header-behaviour leak; no concrete grammar/order/status remains inline."

  # ----------------------------------------------------------------------
  # L0 detector-recoupling (convergence scan round 3).
  # Round 3 widened the L1 (method-call-chain) probe to also catch a
  # whitespace-flanked lifecycle method-name RUN (`init / execute / suspend /
  # teardown`). §4 #27 was drained to a registration-boundary identity + the 5
  # capability invariants + a delegation pointer that NAMES the lifecycle method
  # run only to say its shape is owned by the skill SPI-shape fact +
  # skill-definition.v1.yaml, so the residual the widened probe matches is a
  # regex-indistinguishable DEFENSIBLE delegation pointer (family
  # F-layer-gate-flags-delegation-pointer), the residue the dated allow-list
  # exists to tolerate. Shares the L0 band's 2026-06-30 v1.0-boundary sunset.
  # ----------------------------------------------------------------------

  - id: LPV-l0-skill-spi-lifecycle-run
    layer: L0
    file: architecture/docs/L0/ARCHITECTURE.md
    locus: "§4 #27 Skill-SPI lifecycle delegation block (row-level pass deferred — whole-file: §4 #27 is being rewritten by the concurrent R2/R3 drain, so the lifecycle-run line shifts as the L0 prose moves and a fixed line anchor cannot track it; the widened L1 method-call-chain probe fires on exactly one L0 line — the `init / execute / suspend / teardown` run inside the delegation pointer — so whole-file tolerance scoped to this single category is tight, not a blanket suppression)"
    category: L1-method-call-chain
    trigger: "The concrete *shape* below these invariants — the lifecycle method run (`init` / `execute` / `suspend` / `teardown`), the per-method signatures, and the `SkillContext` / `SkillInvocation` / `SkillResourceMatrix` carriers — is SPI-signature detail owned by the generated fact code-symbol/com-huawei-ascend-middleware-skill-spi-skill public_methods[], the contract skill-definition.v1.yaml, and the capability-ledger key skill_resource_matrix (a delegation pointer that NAMES the migrated lifecycle method run to say its shape is delegated)."
    migrate_to: "architecture/facts/generated/code-symbols.json + docs/contracts/skill-definition.v1.yaml"
    sunset_date: 2026-06-30
    scan_ref: "§1.2 (Skill-SPI band; convergence scan round 3)"
    defensible_part: "The §4 #27 Skill-SPI method shape was drained in-place: the constraint owns only the structural invariants (every external capability is registered via the Skill SPI; release runs unconditionally on the failure path; capacity is admission-validated; UNTRUSTED routes through a sandbox in research/prod; every invocation emits a cost receipt; a suspended Run's skill releases + reconnects) and explicitly delegates the lifecycle method RUN, the per-method signatures, and the SkillContext/SkillInvocation/SkillResourceMatrix carriers to the skill SPI-shape fact's public_methods[] + skill-definition.v1.yaml + the skill_resource_matrix ledger key, 'not this constraint'. The residual the widened L1 method-call-chain probe matches is that DEFENSIBLE delegation pointer NAMING the `init / execute / suspend / teardown` run, regex-indistinguishable from a live method-chain leak; the method shape is already fact-owned + contract-owned. This row is the registration the round-3 detector widening requires so the closed inventory stays coupled to the (now wider) detector."

  # ======================================================================
  # L1 agent-service —the verdict's primary L1 leak site (scan §2.1-§2.4)
  # Sunset 2026-07-31 (aligned with saa-property-authority.yaml grandfather).
  # ======================================================================

  # LPV-l1-as-idempotency-hash-status was REMOVED 2026-05-31 (C4 closeout dead-
  # grandfather removal — see the change-log entry above). Its §2.A hash/collision-
  # status leak was drained; the file's residual L3 (19,397) is covered by
  # LPV-l1-as-idempotency-sql and L4 (94) by LPV-l1-as-runs-api-matrix, so the row
  # tolerated ZERO live finding on either gate (verdict-neutral removal).

  - id: LPV-l1-as-idempotency-sql
    layer: L1
    file: architecture/docs/L1/agent-service/ARCHITECTURE.md
    locus: "§2.A idempotency table, plus the §0 altitude-discipline disclaimer that NAMES SQL / ON CONFLICT / RLS to declare it is NOT carried, plus the §L2-linkage delegation rows that NAME the RLS migration sequence to delegate it (row-level pass deferred — whole-file: the residue the L3 regex matches in this mid-rewrite module-root is the disclaimer + delegation prose, not a live SQL leak)"
    category: L3-sql-rls-persistence
    trigger: "INSERT ... ON CONFLICT (tenant_id, idempotency_key) DO NOTHING; SELECT on collision. Flyway V2__idempotency_dedup.sql ... CHECK constraint on status (CLAIMED|COMPLETED|FAILED)."
    migrate_to: "architecture/docs/L2/fp-idempotency-claim/ + Flyway migrations"
    sunset_date: 2026-07-31
    scan_ref: "§2.1"

  - id: LPV-l1-as-runs-api-matrix
    layer: L1
    file: architecture/docs/L1/agent-service/ARCHITECTURE.md
    locus: "§2.A runs API matrix, plus the §0 altitude-discipline disclaimer that NAMES HTTP routes/verbs/status to declare it is NOT carried, plus the platform/observability frame row that NAMES traceparent behaviour under a per-ADR delegation (row-level pass deferred — whole-file: the residue the L4 regex matches in this mid-rewrite module-root is the disclaimer + the named-header delegation prose, not a live status/route leak)"
    category: L4-http-status-route-verb
    trigger: "POST /v1/runs ... -> 201 ... GET /v1/runs/{runId} -> 200 ... 404 ... POST /v1/runs/{runId}/cancel -> 200 ... 409 illegal_state_transition ... Cancellation is POST, never DELETE ... -> 422 invalid_run_spec ... -> 400 invalid_request ... -> 500"
    migrate_to: "docs/contracts/*.v1.yaml (OpenAPI route x verb x status)"
    sunset_date: 2026-07-31
    scan_ref: "§2.1"

  # LPV-l1-as-tenant-filter-order and LPV-l1-as-observability-filter-wire were
  # REMOVED 2026-05-31 (C4 closeout dead-grandfather removal — see the change-log
  # entry above). Both §2.A filter-order/wire leaks (`order 15 ... 403 tenant_mismatch`;
  # `order 10 ... traceresponse: 00-<trace_id>-...`) were drained; no L5/L6 leak
  # remains in the file and the lone residual L4 token (94) is covered by
  # LPV-l1-as-runs-api-matrix, so each row tolerated ZERO live finding on either gate
  # (verdict-neutral removal).

  # LPV-l1-as-l2-linkage-rls was REMOVED 2026-05-31 (C4 closeout dead-grandfather
  # removal — see the change-log entry above). The `Postgres RLS migration sequence`
  # leak under the in-file 'L2 Constraint Linkage' heading was drained; the file's
  # residual L3 (19,397) is covered by LPV-l1-as-idempotency-sql, so the row
  # tolerated ZERO live finding on either gate (verdict-neutral removal).

  # LPV-l1-as-logical-cas-chain and LPV-l1-as-logical-rls-chain were REMOVED
  # 2026-05-31 (C4 closeout dead-grandfather removal — see the change-log entry
  # above). Both logical.md method-call/CAS leaks (`RunRepository (atomic CAS via
  # updateIfNotTerminal)`; `RunStateMachine.validate(from, to) is invoked`) were
  # drained; no L1-method-call-chain leak remains in logical.md and the residual L3
  # token (169) is covered by LPV-l1-as-logical-rls-tables, so each row tolerated
  # ZERO live finding on either gate (verdict-neutral removal).

  - id: LPV-l1-as-logical-rls-tables
    layer: L1
    file: architecture/docs/L1/agent-service/logical.md
    locus: "§3 tenant-RLS table notes, plus the §5 delegation line that NAMES the RLS policy bodies as owned by development.md §5.3 L2 (row-level pass deferred — whole-file: the residue the L3 regex matches in this mid-rewrite view is the table-note + delegation prose, not a live SQL leak)"
    category: L3-sql-rls-persistence
    trigger: "Every table above carries tenant_id column with RLS policy enabled ... IdempotencyRecord (V2__idempotency_dedup.sql) ships RLS-bound"
    migrate_to: "architecture/docs/L2/fp-tenant-cross-check/ + Flyway migrations"
    sunset_date: 2026-07-31
    scan_ref: "§2.2"

  - id: LPV-l1-as-logical-annotated-dfa
    layer: L1
    file: architecture/docs/L1/agent-service/logical.md
    locus: "176-217 (§3)"
    categories: [L2-runtime-sequence, L1-method-call-chain, L3-sql-rls-persistence, L4-http-status-route-verb]
    trigger: "## 3. Run lifecycle state machine (cancel-race-aware) - a stateDiagram that NAMES the transitions + the cancel-vs-complete race, with 'The response-code posture and the SQL/atomic-primitive realisation are NOT in this view: they are owned by openapi-v1.yaml ... and development.md §5.3' (a delegation pointer that NAMES the migrated CAS/SQL/status detail to say it is delegated)"
    migrate_to: "architecture/docs/L2/fp-run-state-transition/ (annotated state machine) + docs/contracts/openapi-v1.yaml (status)"
    sunset_date: 2026-07-31
    scan_ref: "§2.2"
    defensible_part: "The annotated DFA was drained in-place: the per-edge updateIfNotTerminal CAS, the POST /v1/runs* route verbs, the 409, the RunStateMachine.java:37 source ref, and the WHERE status NOT IN SQL predicate are gone; §3 now carries a NAME-level stateDiagram + the obligation 'every transition is delegated to Layer 2's RunRepository SPI' + a delegation pointer to openapi-v1.yaml (response posture) and development.md §5.3 (CAS realisation). The residual the regex matches is that DEFENSIBLE delegation pointer + the structural state-name vocabulary, not a live runtime-sequence/CAS leak."

  - id: LPV-l1-as-logical-cancel-noop
    layer: L1
    file: architecture/docs/L1/agent-service/logical.md
    locus: "§ RunStateTransitionEvent CAS-no-op note, plus the §ADR cross-reference line that NAMES P3+P6 as covering the cancel-race winner+loser (row-level pass deferred — whole-file: the residue the L2 regex matches in this mid-rewrite view is the event-note + process-name delegation prose, not a live runtime-sequence leak)"
    category: L2-runtime-sequence
    trigger: "RunStateTransitionEvent ... (->CANCELLED) ... (CAS no-op)"
    migrate_to: "architecture/docs/L2/fp-run-state-transition/"
    sunset_date: 2026-07-31
    scan_ref: "§2.2"

  - id: LPV-l1-as-process-sequences
    layer: L1
    file: architecture/docs/L1/agent-service/process.md
    locus: "the P1-P6 process-view section headings + sequenceDiagram blocks that NAME each runtime flow (e.g. '## 3. P3 - Cancel + cancel-race WINNER') (row-level pass deferred — whole-file: the residue the L2 regex matches in this mid-rewrite process view is the flow-NAMING section headings, the defensible process-view construct, not a live runtime-sequence leak)"
    category: L2-runtime-sequence
    trigger: "sequenceDiagram blocks (P1-P6) - six runtime sequence diagrams"
    migrate_to: "architecture/docs/L2/run-http-contract/process.md (process / sequence view)"
    sunset_date: 2026-07-31
    scan_ref: "§2.3 (canonical runtime-sequence leak)"

  - id: LPV-l1-as-process-cancel-race
    layer: L1
    file: architecture/docs/L1/agent-service/process.md
    locus: "211-241 (§6 P6)"
    categories: [L2-runtime-sequence, L4-http-status-route-verb]
    trigger: "## 6. P6 - Cancel-race LOSER (O3 - structural resolution) - a flowchart that NAMES the loser path ('loses - Run already terminal', 'no-op + post-transition re-read'), with 'The loser-side response-code matrix and the rejection-audit CancelRequestedEvent emission are contract material owned by openapi-v1.yaml and run-event.v1.yaml' (a delegation pointer that NAMES the migrated status matrix to say it is delegated)"
    migrate_to: "architecture/docs/L2/fp-run-state-transition/ (race sequence) + docs/contracts/openapi-v1.yaml (status matrix)"
    sunset_date: 2026-07-31
    scan_ref: "§2.3 (verdict's named method-CAS leak)"
    defensible_part: "The P6 winner/loser CAS narrative was drained in-place: the 'CAS WINS ... LOSES because status is now SUCCEEDED' race prose and the inline '=> 409 illegal_state_transition' status are gone; §6 now carries a NAME-level loser-path flowchart + the obligation 'the loser path is deterministic and emits a rejection-audit signal' + a delegation pointer to openapi-v1.yaml (response-code matrix) and run-event.v1.yaml (emission). The residual the regex matches is that DEFENSIBLE process-view section that NAMES the cancel-race flow + delegates the status matrix, not a live runtime-sequence/status leak."

  - id: LPV-l1-as-development-rls-projection
    layer: L1
    file: architecture/docs/L1/agent-service/development.md
    locus: "§5 projection-RLS Boundary Contract, plus the §0 altitude disclaimer NAMING migration/RLS/GUC/CAS to declare it NOT carried, plus the §5.3 zone-realisation delegation that NAMES the RLS outputs to delegate them (row-level pass deferred — whole-file: the residue the L3 regex matches in this mid-rewrite view is the disclaimer + delegation prose, not a live SQL/GUC leak)"
    category: L3-sql-rls-persistence
    trigger: "Tenant-bound RLS coverage extending to the projection tables ... cross-tenant projection blocked at RLS layer"
    migrate_to: "architecture/docs/L2/fp-tenant-cross-check/ + Flyway migrations"
    sunset_date: 2026-07-31
    scan_ref: "§2.4 (RLS inlined into the Boundary Contract)"
    defensible_part: "The §5 Boundary-Contract delegation frame (Rule G-1.1.c - E168) stays; the inlined RLS detail migrates."

  - id: LPV-l1-as-development-rls-guc
    layer: L1
    file: architecture/docs/L1/agent-service/development.md
    locus: "217-236 (§5.3)"
    category: L3-sql-rls-persistence
    trigger: "### 5.3 Zone - Postgres RLS migration sequence (rc25 candidate) - a Boundary Contract that NAMES the delegated outputs ('RLS policies on every tenant-scoped table', 'session-level tenant-GUC wiring', 'RLS-blocked reads counted per tenant') while stating the realisation 'is produced by the L2 design ... deliberately NOT reproduced here' (an obligation-level delegation frame that NAMES the migrated persistence mechanism)"
    migrate_to: "architecture/docs/L2/fp-tenant-cross-check/ + docs/security/rls-policy.sql (design-only) + Flyway migrations"
    sunset_date: 2026-07-31
    scan_ref: "§2.4"
    defensible_part: "The §5 inline RLS realisation was drained in-place: the V?__tenant_rls.sql migration-file series, the literal SET LOCAL app.tenant_id GUC, the R2DBC wiring, and the RLS SELECT-counting prose are gone (line 239-248 is now the §5.4 DualTrackRouter zone, not RLS). §5.3 now publishes the RLS zone as a Rule G-1.1.c Boundary Contract that NAMES the zone identity + obligation-level outputs/DFX and explicitly delegates the realisation to the L2 design + the rls-policy.sql design-only artifact. The residual the L3 regex matches is that DEFENSIBLE delegation frame, not a live SQL/GUC leak."

  # ======================================================================
  # Lower-priority deferred-row + other-module leaks (scan §2.5, §3.2, §3.3)
  # Sunset 2026-08-31.
  # ======================================================================

  - id: LPV-l1-as-physical-rls
    layer: L1
    file: architecture/docs/L1/agent-service/physical.md
    locus: "matched RLS / 3-track / sandbox (row-level pass deferred)"
    category: L3-sql-rls-persistence
    trigger: "matched RLS / 3-track bus / sandbox leak markers (physical view legitimately carries deployment topology; the RLS-policy + per-table detail is the leak)"
    migrate_to: "architecture/docs/L2/fp-tenant-cross-check/"
    sunset_date: 2026-08-31
    scan_ref: "§2.5"
    defensible_part: "Physical-view deployment topology stays (defensible); the RLS-policy + per-table detail migrates."

  # LPV-l1-as-spi-appendix-shapes was REMOVED 2026-05-31 (C4 closeout dead-
  # grandfather removal — see the change-log entry above). spi-appendix.md carries
  # ZERO leak of any category anywhere in the file (the SPI method-body /
  # parameter-list detail was fully drained to docs/contracts/*.v1.yaml +
  # code-symbols.json), so the row tolerated ZERO live finding on either gate
  # (genuine drain, not locus drift; verdict-neutral removal).

  - id: LPV-l1-as-features-cas-rls
    layer: L1
    file: architecture/docs/L1/agent-service/features/session-task-manager.md
    locus: "matched CAS / RLS / sequence (row-level pass deferred; sibling: features/task-centric-control.md)"
    categories: [L1-method-call-chain, L2-runtime-sequence, L3-sql-rls-persistence]
    trigger: "feature notes that NAME the cancel-vs-complete winner/loser arbitration decision table + the process-view P1/P3/P6 sequence delegation pointers (to process.md + development.md §5.3 + ADR-0155 §6) — defensible delegation prose that NAMES the migrated CAS / RLS / runtime-sequence detail to say it is delegated (L2/L3 mixed)"
    migrate_to: "architecture/docs/L2/run-http-contract/process.md"
    sunset_date: 2026-08-31
    scan_ref: "§2.5"

  # LPV-l1-engine-strict-matching-outcome and LPV-l1-middleware-hook-ordering
  # were removed 2026-05-30 (see the change log above): the L1 bus/engine/
  # middleware cleanup drained the dispatch-and-fail outcome chain
  # (agent-execution-engine/ARCHITECTURE.md §3) and the "= registration order"
  # hook-ordering mechanism (agent-middleware/ARCHITECTURE.md §3) out of the L1
  # prose into the contracts + the per-view process narratives, leaving only the
  # boundary identity. The advisory layer-purity gate now scans both files (and
  # their 4+1 views) LEAK-FREE, so neither row suppresses anything; per the
  # closed-list discipline a row that tolerates no live leak is removed, not kept.
